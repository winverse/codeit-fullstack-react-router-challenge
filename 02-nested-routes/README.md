# 챌린지: Nested Routes 연습

## 학습 목표

- 중첩 라우팅을 설정할 수 있다
- `Outlet` 컴포넌트를 사용할 수 있다
- `index` 라우트를 설정할 수 있다

## 해야 할 일

- [ ] `App.jsx`에서 `/products` 경로에 중첩 라우팅 설정하기
- [ ] `ProductsPage`에 `Outlet` 추가하기
- [ ] `/products` 접속 시 기본으로 보여줄 `index` 라우트 설정하기

## 확인하기

- [ ] `/products` 접속 시 ProductsHomePage가 표시되는지 확인
- [ ] `/products/category` 접속 시 CategoryPage가 표시되는지 확인
- [ ] ProductsPage 레이아웃 안에 자식 페이지들이 렌더링되는지 확인
